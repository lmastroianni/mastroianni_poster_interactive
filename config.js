const config = {
    host: "localhost",
    port: 8889, // 3306 for windows/linux or newer macs
    uname: "root",
    pword: "root", // blank for windows
    dbase: "beerstats"
}

module.exports = config;